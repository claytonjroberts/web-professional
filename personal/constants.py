"""Constant values for the personal package."""

import os
import datetime as dt
from pathlib import Path

PATH_TEMPLATES = Path() / "src" / "templates"
PATH_INFO = Path() / "data" / "info.yaml"

# DATETIME_LAST_UPDATE = dt.datetime.fromtimestamp(int(os.environ["DATETIME_LAST_UPDATE"]))
